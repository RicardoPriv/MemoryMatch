import "@/styles/analytics/Title.css"

function Title() {
  return (
    <section id="title-section">
      <div id="title">Pokedex Memory Research</div>
      <div id="subtitle">Select every Pokemon once. Don't pick the same one twice</div>
    </section>
  )
}

export default Title;
